# Legacy launchers

These scripts are historical evidence, not supported execution paths. Use the
root runners and the thin Hamilton wrappers, which require a reserved external
`RUN_ROOT` and a site-provided Basilisk environment.
