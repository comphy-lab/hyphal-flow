# Legacy constitutive headers

These elastic-only variants are retained for provenance and are not included
by the canonical simulation. The supported constitutive path is
`../three-phase-rheology.h` plus `../log-conformation-rheology.h`.
